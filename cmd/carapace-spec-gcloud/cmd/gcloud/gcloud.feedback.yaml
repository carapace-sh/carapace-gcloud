# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: feedback
description: Provide feedback to the Google Cloud CLI team.
flags:
    --log-file=: Path to the log file from a prior gcloud run.
