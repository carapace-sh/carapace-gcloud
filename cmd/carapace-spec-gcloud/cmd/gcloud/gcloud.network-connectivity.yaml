# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: network-connectivity
description: Manage Network Connectivity resources.
commands:
    - name: hubs
      description: Manage Network Connectivity Center hubs.
      commands:
        - name: list
          description: List hubs.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
        - name: reject-spoke
          description: Reject a spoke from a hub.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --details=: Additional details behind the rejection
            --no-async&: Return immediately, without waiting for the operation in progress to
            --spoke=!: URI of the spoke to reject
        - name: set-iam-policy
          description: Set the IAM policy of a hub resource.
        - name: accept-spoke
          description: Accept a spoke into a hub.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --no-async&: Return immediately, without waiting for the operation in progress to
            --spoke=!: URI of the spoke to accept
        - name: get-iam-policy
          description: Get the IAM policy for a hub resource.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
        - name: groups
          description: Manage Network Connectivity Center groups.
          commands:
            - name: remove-iam-policy-binding
              description: Remove an IAM policy binding from the IAM policy of a group resource.
              flags:
                --hub=: Id of the hub.
                --member=!: The principal to remove the binding for.
                --role=!: The role to remove the principal from.
            - name: set-iam-policy
              description: Set the IAM policy of a group resource.
              flags:
                --hub=: Id of the hub.
            - name: update
              description: Update a group.
              flags:
                --add-auto-accept-projects=: Append the given values to the current auto-accept projects.
                --async: Return immediately, without waiting for the operation in progress to
                --clear-auto-accept-projects: Empty the current auto-accept projects.
                --clear-labels: Remove all labels.
                --description=: New description of the group.
                --hub=: The hub Id.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --no-clear-auto-accept-projects&: Empty the current auto-accept projects.
                --no-clear-labels&: Remove all labels.
                --remove-auto-accept-projects=: Remove the given values from the current auto-accept projects.
                --remove-labels=: List of label keys to remove.
                --update-labels=: List of label KEY=VALUE pairs to update.
            - name: add-iam-policy-binding
              description: Add an IAM policy binding to the IAM policy of a group resource.
              flags:
                --hub=: Id of the hub.
                --member=!: The principal to add the binding for.
                --role=!: Role name to assign to the principal.
            - name: describe
              description: Describe a group.
              flags:
                --hub=: Id of the hub.
            - name: get-iam-policy
              description: Get the IAM policy for a group resource.
              flags:
                --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
                --hub=: Id of the hub.
                --limit=: Maximum number of resources to list.
                --page-size=: Some services group resource list output into pages.
                --sort-by=: Comma-separated list of resource field key names to sort by.
            - name: list
              description: List groups.
              flags:
                --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
                --hub=: ID of the hub or fully qualified identifier for the hub.
                --limit=: Maximum number of resources to list.
                --page-size=: Some services group resource list output into pages.
                --sort-by=: Comma-separated list of resource field key names to sort by.
        - name: list-spokes
          description: List hub spokes.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --spoke-locations=: A comma separated list of locations.
            --view=: Enumeration to control which spoke fields are included in the response.
          completion:
            flag:
                view:
                    - basic
                    - detailed
        - name: query-status
          description: Query the status of Private Service Connect propagation for a hub.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --group-by=: Comma-separated list of resource field key names to group by.
            --limit=: Maximum number of resources to list.
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
        - name: reject-spoke-update
          description: Reject a proposal to update a spoke in a hub.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --details=: Additional details behind the rejection
            --no-async&: Return immediately, without waiting for the operation in progress to
            --spoke-etag=!: Etag of the spoke to reject update
            --spoke=!: URI of the spoke to reject update
        - name: update
          description: Update a hub.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --clear-labels: Remove all labels.
            --description=: New description of the hub.
            --export-psc: Whether Private Service Connect transitivity is enabled for the hub.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-clear-labels&: Remove all labels.
            --no-export-psc&: Whether Private Service Connect transitivity is enabled for the hub.
            --remove-labels=: List of label keys to remove.
            --update-labels=: List of label KEY=VALUE pairs to update.
        - name: accept-spoke-update
          description: Accept a proposal to update a spoke in a hub.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --no-async&: Return immediately, without waiting for the operation in progress to
            --spoke-etag=!: Etag of the spoke to accept update
            --spoke=!: URI of the spoke to accept update
        - name: add-iam-policy-binding
          description: Add an IAM policy binding to the IAM policy of a hub resource.
          flags:
            --member=!: The principal to add the binding for.
            --role=!: Role name to assign to the principal.
        - name: describe
          description: Describe a hub.
        - name: remove-iam-policy-binding
          description: Remove an IAM policy binding from the IAM policy of a hub resource.
          flags:
            --member=!: The principal to remove the binding for.
            --role=!: The role to remove the principal from.
        - name: create
          description: Create a new hub.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --description=: Description of the hub.
            --export-psc: This boolean controls whether Private Service Connect transitivity is enabled for the hub.
            --labels=: List of label KEY=VALUE pairs to add.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-export-psc&: This boolean controls whether Private Service Connect transitivity is enabled for the hub.
            --policy-mode=: Policy mode of the hub.
            --preset-topology=: Topology of the hub.
          completion:
            flag:
                policy-mode:
                    - policy-mode-unspecified
                    - preset
                preset-topology:
                    - hybrid-inspection
                    - mesh
                    - preset-topology-unspecified
                    - star
        - name: route-tables
          description: Manage Network Connectivity Center route tables.
          commands:
            - name: describe
              description: Describe a route table.
              flags:
                --hub=: Id of the hub.
            - name: list
              description: List route tables.
              flags:
                --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
                --hub=: ID of the hub or fully qualified identifier for the hub.
                --limit=: Maximum number of resources to list.
                --page-size=: Some services group resource list output into pages.
                --sort-by=: Comma-separated list of resource field key names to sort by.
            - name: routes
              description: Manage Network Connectivity Center routes.
              commands:
                - name: describe
                  description: Describe a route.
                  flags:
                    --hub=: Id of the hub.
                    --route-table=: Id of the route table.
                - name: list
                  description: List routes.
                  flags:
                    --effective-location=: The effective location/region to limit the list of routes.
                    --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
                    --hub=: Id of the hub.
                    --limit=: Maximum number of resources to list.
                    --page-size=: Some services group resource list output into pages.
                    --route-table=: ID of the route table or fully qualified identifier for the route table.
                    --sort-by=: Comma-separated list of resource field key names to sort by.
        - name: delete
          description: Delete a hub.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --no-async&: Return immediately, without waiting for the operation in progress to
    - name: internal-ranges
      description: Manage internal ranges.
      commands:
        - name: list
          description: List internal ranges.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --page-size=: Some services group resource list output into pages.
            --region=: ID of the region or fully qualified identifier for the region.
            --sort-by=: Comma-separated list of resource field key names to sort by.
        - name: update
          description: Update an internal range.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --clear-labels: Clear existing labels for the range.
            --clear-overlaps: Clear existing overlap fields for the range.
            --description=: Description of the Internal Range
            --ip-cidr-range=: IP range that this internal range defines.
            --labels=: List of label KEY=VALUE pairs for the range being updated.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-clear-labels&: Clear existing labels for the range.
            --no-clear-overlaps&: Clear existing overlap fields for the range.
            --overlaps=: Overlap specifications for the range being updated.
            --prefix-length=: An alternative to ip-cidr-range.
            --region=: The location ID.
        - name: create
          description: Create a new internal range.
          flags:
            --allocation-strategy=: Allocation strategy to be used when searching for a free range.
            --async: Return immediately, without waiting for the operation in progress to
            --description=: Description of the internal range to be created.
            --exclude-cidr-ranges=: A list of CIDR ranges to exclude from the search for a free range.
            --first-available-ranges-lookup-size=: The number of ranges to be considered when using the
            --immutable: Mark the internal range as immutable.
            --ip-cidr-range=: IP range that this internal range defines.
            --labels=: List of label KEY=VALUE pairs to add.
            --migration-source=!: The migration source subnetwork (of a for-migration internal range) resource URI or
            --migration-target=!: The migration target subnetwork (of a for-migration internal range) resource URI or
            --network=!: The URL or resource ID of the network in which to reserve the internal range.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-immutable&: Mark the internal range as immutable.
            --overlaps=: Overlap specifications for the range being created.
            --peering=: The type of peering set for the internal range.
            --prefix-length=!: An alternative to ip-cidr-range.
            --region=: The location ID.
            --target-cidr-range=: Can be set to narrow down or pick a different address space while searching for
            --usage=: The type of usage set for the internal range.
          completion:
            flag:
                allocation-strategy:
                    - first-available
                    - first-smallest-fitting
                    - random
                    - random-first-n-available
                peering:
                    - for-peer
                    - for-self
                    - not-shared
                usage:
                    - external-to-vpc
                    - for-migration
                    - for-vpc
        - name: delete
          description: Delete an internal range.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --no-async&: Return immediately, without waiting for the operation in progress to
            --region=: The location ID.
        - name: describe
          description: Describe an internal range.
          flags:
            --region=: The location ID.
    - name: locations
      description: Get information about supported Network Connectivity Center locations.
      commands:
        - name: describe
          description: Describe a Network Connectivity Center location.
        - name: list
          description: List Network Connectivity Center locations.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
    - name: multicloud-data-transfer-configs
      description: Manage Multicloud Data Transfer Config resources.
      commands:
        - name: create
          description: Create a multicloudDataTransferConfig.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --description=: A description of this resource.
            --etag=: The etag is computed by the server, and might be sent with update and
            --labels=: User-defined labels.
            --location=: The location id of the multicloudDataTransferConfig resource.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --request-id=: A request ID to identify requests.
            --services=: Maps services to their current or planned states.
        - name: delete
          description: Delete a multicloudDataTransferConfig.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --etag=: The etag is computed by the server, and might be sent with update and
            --location=: The location id of the multicloudDataTransferConfig resource.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --request-id=: A request ID to identify requests.
        - name: describe
          description: Describe a multicloudDataTransferConfig.
          flags:
            --location=: The location id of the multicloudDataTransferConfig resource.
        - name: destinations
          description: Manage Destination resources.
          commands:
            - name: create
              description: Create a destination.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --description=: A description of this resource.
                --endpoints=!: Required, The list of `DestinationEndpoint` resources configured for the IP prefix.
                --etag=: The etag is computed by the server, and might be sent with update and
                --ip-prefix=!: The IP prefix that represents your workload on another CSP.
                --labels=: User-defined labels.
                --location=: The location id of the destination resource.
                --multicloud-data-transfer-config=: The multicloudDataTransferConfig id of the destination resource.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --request-id=: A request ID to identify requests.
            - name: delete
              description: Delete a destination.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --etag=: The etag is computed by the server, and might be sent with update and
                --location=: The location id of the destination resource.
                --multicloud-data-transfer-config=: The multicloudDataTransferConfig id of the destination resource.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --request-id=: A request ID to identify requests.
            - name: describe
              description: Describe a destination.
              flags:
                --location=: The location id of the destination resource.
                --multicloud-data-transfer-config=: The multicloudDataTransferConfig id of the destination resource.
            - name: list
              description: List destinations.
              flags:
                --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
                --limit=: Maximum number of resources to list.
                --location=: The location id of the multicloudDataTransferConfig resource.
                --multicloud-data-transfer-config=!: ID of the multicloudDataTransferConfig or fully qualified identifier for the multicloudDataTransferConfig.
                --no-return-partial-success&: If `true`, allow partial responses for multi-regional aggregated list
                --no-uri&: Print a list of resource URIs instead of the default output, and change the
                --page-size=: Some services group resource list output into pages.
                --return-partial-success: If `true`, allow partial responses for multi-regional aggregated list
                --sort-by=: Comma-separated list of resource field key names to sort by.
                --uri: Print a list of resource URIs instead of the default output, and change the
            - name: update
              description: Update a destination.
              flags:
                --add-endpoints=: Add new value to endpoints list.
                --async: Return immediately, without waiting for the operation in progress to
                --clear-endpoints: Clear endpoints value and set to empty list.
                --clear-labels: Clear labels value and set to empty map.
                --description=: A description of this resource.
                --endpoints=: Set endpoints to new value.
                --etag=: The etag is computed by the server, and might be sent with update and
                --labels=: Set labels to new value.
                --location=: The location id of the destination resource.
                --multicloud-data-transfer-config=: The multicloudDataTransferConfig id of the destination resource.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --no-clear-endpoints&: Clear endpoints value and set to empty list.
                --no-clear-labels&: Clear labels value and set to empty map.
                --remove-endpoints=: Remove existing value from endpoints list.
                --remove-labels=: Remove existing value from map labels.
                --request-id=: A request ID to identify requests.
                --update-labels=: Update labels value or add key value pair.
        - name: list
          description: List multicloudDataTransferConfigs.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-return-partial-success&: If `true`, allows partial responses for multi-regional aggregated list
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --return-partial-success: If `true`, allows partial responses for multi-regional aggregated list
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
        - name: update
          description: Update a multicloudDataTransferConfig.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --clear-labels: Clear labels value and set to empty map.
            --clear-services: Clear services value and set to empty map.
            --description=: A description of this resource.
            --etag=: The etag is computed by the server, and might be sent with update and
            --labels=: Set labels to new value.
            --location=: The location id of the multicloudDataTransferConfig resource.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-clear-labels&: Clear labels value and set to empty map.
            --no-clear-services&: Clear services value and set to empty map.
            --remove-labels=: Remove existing value from map labels.
            --remove-services=: Remove existing value from map services.
            --request-id=: A request ID to identify requests.
            --services=: Set services to new value.
            --update-labels=: Update labels value or add key value pair.
            --update-services=: Update services value or add key value pair.
    - name: multicloud-data-transfer-supported-services
      description: Manage Multicloud Data Transfer Supported Service resources.
      commands:
        - name: describe
          description: Describe multicloudDataTransferSupportedService.
          flags:
            --location=: The location id of the multicloudDataTransferSupportedService resource.
        - name: list
          description: List multicloudDataTransferSupportedServices.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
    - name: operations
      description: Manage Network Connectivity Center operations.
      commands:
        - name: describe
          description: Describe a Network Connectivity Center operation.
          flags:
            --region=: The location Id.
        - name: list
          description: List Network Connectivity Center operations.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --page-size=: Some services group resource list output into pages.
            --region=!: ID of the region or fully qualified identifier for the region.
            --sort-by=: Comma-separated list of resource field key names to sort by.
    - name: regional-endpoints
      description: Manage Network Connectivity RegionalEndpoints.
      commands:
        - name: describe
          description: Describe a regional endpoint.
          flags:
            --region=: The location Id.
        - name: list
          description: List regional endpoints.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --page-size=: Some services group resource list output into pages.
            --region=!: ID of the region or fully qualified identifier for the region.
            --sort-by=: Comma-separated list of resource field key names to sort by.
        - name: create
          description: Create a new regional endpoint.
          flags:
            --address=: The IP Address of the Regional Endpoint.
            --async: Return immediately, without waiting for the operation in progress to
            --description=: Description of the regional endpoint.
            --enable-global-access: Whether the REGIONAL or GLOBAL access is enabled.
            --labels=: List of label KEY=VALUE pairs to add.
            --network=: Consumer's VPC network that this regional endpoint belongs to.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-enable-global-access&: Whether the REGIONAL or GLOBAL access is enabled.
            --region=: The location Id.
            --subnetwork=: The name of the subnetwork from which the IP address will be allocated.
            --target-google-api=!: The service endpoint the regional endpoint will connect to.
        - name: delete
          description: Delete a regional endpoint.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --no-async&: Return immediately, without waiting for the operation in progress to
            --region=: The location Id.
    - name: service-connection-policies
      description: Manage Service Connection Policies.
      commands:
        - name: describe
          description: Describe a service connection policy.
          flags:
            --region=: The location Id.
        - name: list
          description: List service connection policies.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --page-size=: Some services group resource list output into pages.
            --region=!: ID of the region or fully qualified identifier for the region.
            --sort-by=: Comma-separated list of resource field key names to sort by.
        - name: update
          description: Update a service connection policy.
          flags:
            --allowed-google-producers-resource-hierarchy-level=: List of projects, folders, or orgs where the producer instance can be located in the form "projects/123456789", folders/123456789", or "organizations/123456789".
            --async: Return immediately, without waiting for the operation in progress to
            --description=: Description of the Service Connection Policy to be updated.
            --labels=: List of label KEY=VALUE pairs to add.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --producer-instance-location=: Option that determines where the producer instances can be located for which connections can be created in the network controlled by this policy.
            --psc-connection-limit=: Max number of PSC connections for this policy.
            --region=: For resources [service_connection_policy, subnets], provides fallback value for resource region attribute.
            --subnets=!: IDs of the subnetworks or fully qualified identifiers for the subnetworks.
          completion:
            flag:
                producer-instance-location:
                    - custom-resource-hierarchy-levels
                    - none
        - name: create
          description: Create a new Service Connection Policy.
          flags:
            --allowed-google-producers-resource-hierarchy-level=: List of projects, folders, or orgs where the producer instance can be located in the form "projects/123456789", folders/123456789", or "organizations/123456789".
            --async: Return immediately, without waiting for the operation in progress to
            --description=: Description of the Service Connection Policy to be created.
            --labels=: List of label KEY=VALUE pairs to add.
            --network=!: ID of the network or fully qualified identifier for the network.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --producer-instance-location=: Option that determines where the producer instances can be located for which connections can be created in the network controlled by this policy.
            --psc-connection-limit=: Max number of PSC connections for this policy.
            --region=: For resources [service_connection_policy, subnets], provides fallback value for resource region attribute.
            --service-class=!: Service class that this policy is created for.
            --subnets=!: IDs of the subnetworks or fully qualified identifiers for the subnetworks.
          completion:
            flag:
                producer-instance-location:
                    - custom-resource-hierarchy-levels
                    - none
        - name: delete
          description: Delete a service connection policy.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --no-async&: Return immediately, without waiting for the operation in progress to
            --region=: The location Id.
    - name: policy-based-routes
      description: Manage Policy-based Routes.
      commands:
        - name: delete
          description: Delete a policy-based route.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: describe
          description: Describe a policy-based route.
        - name: list
          description: List policy-based routes.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
        - name: create
          description: Create a new policy-based route.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --description=: Optional description of this resource.
            --destination-range=: Destination IP range of outgoing packets that this policy-based route applies to.
            --interconnect-attachment-region=: Cloud region to install this policy-based route on interconnect attachment.
            --ip-protocol=: IP protocol that this policy-based route applies to.
            --labels=: List of label KEY=VALUE pairs to add.
            --network=!: Fully-qualified URL of the network that this route applies to.
            --next-hop-ilb-ip=: IP of a global access enabled L4 ILB that should be the next hop to handle packets.
            --next-hop-other-routes=: Next hop should be other routes that handle packets.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --priority=: Priority of this policy-based route.
            --protocol-version=: Internet protocol versions that this policy-based route applies to.
            --source-range=: Source IP range of outgoing packets that this policy-based route applies to.
            --tags=: List of VM instance tags that this route applies to.
          completion:
            flag:
                next-hop-other-routes:
                    - default-routing
                    - other-routes-unspecified
                protocol-version:
                    - ipv4
                    - ipv6
                    - protocol-version-unspecified
    - name: spokes
      description: Manage Network Connectivity Center spokes.
      commands:
        - name: activate
          description: Activate a spoke.
          hidden: true
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --global&: Indicates that the spoke is global.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --region=: A Google Cloud region.
        - name: deactivate
          description: Deactivate a spoke.
          hidden: true
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --global&: Indicates that the spoke is global.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --region=: A Google Cloud region.
        - name: delete
          description: Delete a spoke.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --global: Indicates that the spoke is global.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --region=: A Google Cloud region.
        - name: describe
          description: Describe a spoke.
          flags:
            --global: Indicates that the spoke is global.
            --region=: A Google Cloud region.
        - name: linked-interconnect-attachments
          description: Manage VLAN attachment spokes.
          commands:
            - name: create
              description: Create a new VLAN attachment spoke.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --description=: Description of the spoke to create.
                --exclude-export-ranges=&: Dynamic routes overlapped/encompassed by exclude export ranges are excluded during export to hub.
                --exclude-import-ranges=&: Hub routes overlapped/encompassed by exclude import ranges are excluded during import from hub.
                --group=: The group that the spoke will be added to.
                --hub=!: Hub that the spoke will attach to.
                --include-export-ranges=&: Dynamic routes fully encompassed by include export ranges are included during export to hub.
                --include-import-ranges=: IP address range(s) allowed to be imported from hub subnets.
                --interconnect-attachments=!: VLAN attachments that the spoke provides connectivity to.
                --labels=: List of label KEY=VALUE pairs to add.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --no-site-to-site-data-transfer&: Whether to enable site-to-site data transfer for this spoke.
                --region=: The location Id.
                --site-to-site-data-transfer: Whether to enable site-to-site data transfer for this spoke.
            - name: update
              description: Update a VLAN attachment spoke.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --clear-labels: Remove all labels.
                --description=: New description of the spoke.
                --exclude-export-ranges=&: Dynamic routes overlapped/encompassed by exclude export ranges are excluded during export to hub.
                --exclude-import-ranges=&: Hub routes overlapped/encompassed by exclude import ranges are excluded during import from hub.
                --include-export-ranges=&: Dynamic routes fully encompassed by include export ranges are included during export to hub.
                --include-import-ranges=: IP address range(s) allowed to be imported from hub subnets.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --no-clear-labels&: Remove all labels.
                --region=: The location Id.
                --remove-labels=: List of label keys to remove.
                --update-labels=: List of label KEY=VALUE pairs to update.
        - name: linked-router-appliances
          description: Manage Router appliance spokes.
          commands:
            - name: create
              description: Create a new Router appliance spoke.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --description=: Description of the spoke to create.
                --exclude-export-ranges=&: Dynamic routes overlapped/encompassed by exclude export ranges are excluded during export to hub.
                --exclude-import-ranges=&: Hub routes overlapped/encompassed by exclude import ranges are excluded during import from hub.
                --group=: The group that the spoke will be added to.
                --hub=!: Hub that the spoke will attach to.
                --include-export-ranges=&: Dynamic routes fully encompassed by include export ranges are included during export to hub.
                --include-import-ranges=: IP address range(s) allowed to be imported from hub subnets.
                --labels=: List of label KEY=VALUE pairs to add.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --no-site-to-site-data-transfer&: Whether to enable site-to-site data transfer for this spoke.
                --region=: The location Id.
                --router-appliance=!: Router appliance instance(s) that the spoke provides connectivity to.
                --site-to-site-data-transfer: Whether to enable site-to-site data transfer for this spoke.
            - name: update
              description: Update a Router appliance spoke.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --clear-labels: Remove all labels.
                --description=: New description of the spoke.
                --exclude-export-ranges=&: Dynamic routes overlapped/encompassed by exclude export ranges are excluded during export to hub.
                --exclude-import-ranges=&: Hub routes overlapped/encompassed by exclude import ranges are excluded during import from hub.
                --include-export-ranges=&: Dynamic routes fully encompassed by include export ranges are included during export to hub.
                --include-import-ranges=: IP address range(s) allowed to be imported from hub subnets.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --no-clear-labels&: Remove all labels.
                --region=: The location Id.
                --remove-labels=: List of label keys to remove.
                --router-appliance=: Router appliance instance(s) with which to replace the set of instances already
                --update-labels=: List of label KEY=VALUE pairs to update.
        - name: linked-vpn-tunnels
          description: Manage VPN spokes.
          commands:
            - name: create
              description: Create a new VPN spoke.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --description=: Description of the spoke to create.
                --exclude-export-ranges=&: Dynamic routes overlapped/encompassed by exclude export ranges are excluded during export to hub.
                --exclude-import-ranges=&: Hub routes overlapped/encompassed by exclude import ranges are excluded during import from hub.
                --group=: The group that the spoke will be added to.
                --hub=!: Hub that the spoke will attach to.
                --include-export-ranges=&: Dynamic routes fully encompassed by include export ranges are included during export to hub.
                --include-import-ranges=: IP address range(s) allowed to be imported from hub subnets.
                --labels=: List of label KEY=VALUE pairs to add.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --no-site-to-site-data-transfer&: Whether to enable site-to-site data transfer for this spoke.
                --region=: The location Id.
                --site-to-site-data-transfer: Whether to enable site-to-site data transfer for this spoke.
                --vpn-tunnels=!: HA VPN tunnels that the spoke provides connectivity to.
            - name: update
              description: Update a VPN spoke.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --clear-labels: Remove all labels.
                --description=: New description of the spoke.
                --exclude-export-ranges=&: Dynamic routes overlapped/encompassed by exclude export ranges are excluded during export to hub.
                --exclude-import-ranges=&: Hub routes overlapped/encompassed by exclude import ranges are excluded during import from hub.
                --include-export-ranges=&: Dynamic routes fully encompassed by include export ranges are included during export to hub.
                --include-import-ranges=: IP address range(s) allowed to be imported from hub subnets.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --no-clear-labels&: Remove all labels.
                --region=: The location Id.
                --remove-labels=: List of label keys to remove.
                --update-labels=: List of label KEY=VALUE pairs to update.
        - name: list
          description: List spokes.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --global: Indicates that the spoke is global.
            --limit=: Maximum number of resources to list.
            --page-size=: Some services group resource list output into pages.
            --region=: A Google Cloud region.
            --sort-by=: Comma-separated list of resource field key names to sort by.
        - name: linked-producer-vpc-network
          description: Manage Producer VPC spokes.
          commands:
            - name: create
              description: Create a new Producer VPC spoke.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --description=: Description of the spoke to create.
                --exclude-export-ranges=: Subnet IP address range(s) to hide from other VPC networks that are
                --global: Indicates that the spoke is global.
                --group=&: Group that the spoke will be part of.
                --hub=!: Hub that the spoke will attach to.
                --include-export-ranges=: Subnet IP address range(s) to export to other VPC networks that are
                --labels=: List of label KEY=VALUE pairs to add.
                --network=!: Your VPC network that contains the peering to the Producer VPC,
                --no-async&: Return immediately, without waiting for the operation in progress to
                --peering=!: Peering between your network and the Producer VPC, which this
                --region=&: A Google Cloud region.
            - name: update
              description: Update a Producer VPC spoke.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --clear-labels: Remove all labels.
                --description=: New description of the spoke.
                --exclude-export-ranges=: New exclude export ranges of the spoke.
                --global: Indicates that the spoke is global.
                --include-export-ranges=: New include export ranges of the spoke.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --no-clear-labels&: Remove all labels.
                --region=&: A Google Cloud region.
                --remove-labels=: List of label keys to remove.
                --update-labels=: List of label KEY=VALUE pairs to update.
        - name: linked-vpc-network
          description: Manage VPC spokes.
          commands:
            - name: create
              description: Create a new VPC spoke.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --description=: Description of the spoke to create.
                --exclude-export-ranges=: Subnet IP address range(s) to hide from other VPC networks that are
                --global: Indicates that the spoke is global.
                --group=&: Group that the spoke will be part of.
                --hub=!: Hub that the spoke will attach to.
                --include-export-ranges=: Subnet IP address range(s) to export to other VPC networks that are
                --labels=: List of label KEY=VALUE pairs to add.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --region=&: A Google Cloud region.
                --vpc-network=!: VPC network that the spoke provides connectivity to.
            - name: update
              description: Update a VPC spoke.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --clear-labels: Remove all labels.
                --description=: New description of the spoke.
                --exclude-export-ranges=: New exclude export ranges of the spoke.
                --global: Indicates that the spoke is global.
                --include-export-ranges=: New include export ranges of the spoke.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --no-clear-labels&: Remove all labels.
                --region=&: A Google Cloud region.
                --remove-labels=: List of label keys to remove.
                --update-labels=: List of label KEY=VALUE pairs to update.
