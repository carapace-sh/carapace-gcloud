# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: compliance-manager
description: Manage Compliance Manager resources.
commands:
    - name: framework-deployments
      description: Manage Framework Deployment resources.
      commands:
        - name: create
          description: Create a framework deployment.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --cloud-control-metadata=!: Required, The deployment mode and parameters for each of the cloud controls in
            --description=: A user-provided description of the framework deployment.
            --etag=: To prevent concurrent updates from overwriting each other, always provide
            --framework-major-revision-id=: The major version of the framework.
            --framework=!: The major version of the framework.
            --location=: The location id of the frameworkDeployment resource.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --organization=: The organization id of the frameworkDeployment resource.
            --target-resource-config-existing=: 'The resource hierarchy node, in one of the following formats:'
            --target-resource-creation-config-folder-display-name=!: The display name of the folder.
            --target-resource-creation-config-folder-parent=!: The parent of the folder, in the format
            --target-resource-creation-config-project-billing-account-id=!: The billing account ID for the project.
            --target-resource-creation-config-project-display-name=!: The display name of the project.
            --target-resource-creation-config-project-parent=!: The parent of the project, in the format
        - name: delete
          description: Delete a framework deployment.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --etag=: An opaque identifier for the current version of the resource.
            --location=: The location id of the frameworkDeployment resource.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --organization=: The organization id of the frameworkDeployment resource.
        - name: describe
          description: Get a framework deployment.
          flags:
            --location=: The location id of the frameworkDeployment resource.
            --organization=: The organization id of the frameworkDeployment resource.
        - name: list
          description: List framework deployments.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --organization=: The organization id of the location resource.
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
    - name: frameworks
      description: Manage Framework resources.
      commands:
        - name: create
          description: Create a framework.
          flags:
            --category=: The category of the framework.
            --cloud-control-details=: The cloud control details that are directly added without any
            --description=: The description of the framework.
            --display-name=: The friendly name of the framework.
            --location=: The location id of the framework resource.
            --organization=: The organization id of the framework resource.
        - name: delete
          description: Delete a framework.
          flags:
            --location=: The location id of the framework resource.
            --organization=: The organization id of the framework resource.
        - name: describe
          description: Get a framework.
          flags:
            --location=: The location id of the framework resource.
            --major-revision-id=: The framework major version to retrieve.
            --organization=: The organization id of the framework resource.
        - name: list
          description: List frameworks.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --organization=: The organization id of the location resource.
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
        - name: update
          description: Update a framework.
          hidden: true
          flags:
            --add-category=: Add new value to category list.
            --add-cloud-control-details=: Add new value to cloud_control_details list.
            --category=: Set category to new value.
            --clear-category: Clear category value and set to empty list.
            --clear-cloud-control-details: Clear cloud_control_details value and set to empty list.
            --cloud-control-details=: Set cloud_control_details to new value.
            --description=: The description of the framework.
            --display-name=: The friendly name of the framework.
            --location=: The location id of the framework resource.
            --major-revision-id=: The major version ID of the framework to update.
            --no-clear-category&: Clear category value and set to empty list.
            --no-clear-cloud-control-details&: Clear cloud_control_details value and set to empty list.
            --organization=: The organization id of the framework resource.
            --remove-category=: Remove existing value from category list.
            --remove-cloud-control-details=: Remove existing value from cloud_control_details list.
    - name: operations
      description: Manage Operation resources.
      commands:
        - name: describe
          description: Describe operations.
          flags:
            --location=: The location id of the operation resource.
            --organization=: The organization id of the operation resource.
        - name: list
          description: List operations.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --organization=: The organization id of the location resource.
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
        - name: wait
          description: Wait operations.
          flags:
            --location=: The location id of the operation resource.
            --organization=: The organization id of the operation resource.
        - name: cancel
          description: Cancel operations.
          flags:
            --location=: The location id of the operation resource.
            --organization=: The organization id of the operation resource.
        - name: delete
          description: Delete operations.
          flags:
            --location=: The location id of the operation resource.
            --organization=: The organization id of the operation resource.
    - name: cloud-control-deployments
      description: Manage Cloud Control Deployment resources.
      commands:
        - name: describe
          description: Get a cloud control deployment.
          flags:
            --location=: The location id of the cloudControlDeployment resource.
            --organization=: The organization id of the cloudControlDeployment resource.
        - name: list
          description: List Cloud Control Deployments.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --organization=: The organization id of the location resource.
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
    - name: cloud-controls
      description: Manage Cloud Control resources.
      commands:
        - name: update
          description: Update a cloud control.
          hidden: true
          flags:
            --add-categories=: Add new value to categories list.
            --add-parameter-spec=: Add new value to parameter_spec list.
            --add-rules=: Add new value to rules list.
            --add-supported-cloud-providers=: Add new value to supported_cloud_providers list.
            --add-supported-target-resource-types=: Add new value to supported_target_resource_types list.
            --categories=: Set categories to new value.
            --clear-categories: Clear categories value and set to empty list.
            --clear-parameter-spec: Clear parameter_spec value and set to empty list.
            --clear-rules: Clear rules value and set to empty list.
            --clear-supported-cloud-providers: Clear supported_cloud_providers value and set to empty list.
            --clear-supported-target-resource-types: Clear supported_target_resource_types value and set to empty list.
            --description=: A description of the cloud control.
            --display-name=: The friendly name of the cloud control.
            --finding-category=: The finding category for the cloud control findings.
            --location=: The location id of the cloudControl resource.
            --no-clear-categories&: Clear categories value and set to empty list.
            --no-clear-parameter-spec&: Clear parameter_spec value and set to empty list.
            --no-clear-rules&: Clear rules value and set to empty list.
            --no-clear-supported-cloud-providers&: Clear supported_cloud_providers value and set to empty list.
            --no-clear-supported-target-resource-types&: Clear supported_target_resource_types value and set to empty list.
            --organization=: The organization id of the cloudControl resource.
            --parameter-spec=: Set parameter_spec to new value.
            --remediation-steps=: The remediation steps for the cloud control findings.
            --remove-categories=: Remove existing value from categories list.
            --remove-parameter-spec=: Remove existing value from parameter_spec list.
            --remove-rules=: Remove existing value from rules list.
            --remove-supported-cloud-providers=: Remove existing value from supported_cloud_providers list.
            --remove-supported-target-resource-types=: Remove existing value from supported_target_resource_types list.
            --rules=: Set rules to new value.
            --severity=: The severity of the findings that are generated by the cloud control.
            --supported-cloud-providers=: Set supported_cloud_providers to new value.
            --supported-target-resource-types=: Set supported_target_resource_types to new value.
          completion:
            flag:
                severity:
                    - critical
                    - high
                    - low
                    - medium
        - name: create
          description: Create a cloud control.
          flags:
            --categories=: The categories for the cloud control.
            --description=: A description of the cloud control.
            --display-name=: The friendly name of the cloud control.
            --finding-category=: The finding category for the cloud control findings.
            --location=: The location id of the cloudControl resource.
            --organization=: The organization id of the cloudControl resource.
            --parameter-spec=: The parameter specifications for the cloud control.
            --remediation-steps=: The remediation steps for the cloud control findings.
            --rules=: The rules that you can enforce to meet your security or compliance intent.
            --severity=: The severity of the findings that are generated by the cloud control.
            --supported-cloud-providers=: The supported cloud providers.
            --supported-target-resource-types=: The target resource types that are supported by the cloud control.
          completion:
            flag:
                severity:
                    - critical
                    - high
                    - low
                    - medium
        - name: delete
          description: Delete a cloud control.
          flags:
            --location=: The location id of the cloudControl resource.
            --organization=: The organization id of the cloudControl resource.
        - name: describe
          description: Get a cloud control.
          flags:
            --location=: The location id of the cloudControl resource.
            --major-revision-id=: The major version of the cloud control to retrieve.
            --organization=: The organization id of the cloudControl resource.
        - name: list
          description: List cloud controls.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --organization=: The organization id of the location resource.
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
