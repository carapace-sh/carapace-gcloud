# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: edge-cloud
description: Manage edge-cloud resources.
commands:
    - name: container
      description: Manage Edge Container resources.
      commands:
        - name: clusters
          description: Manage Kubernetes Edge clusters.
          commands:
            - name: create
              description: Create an Edge Container cluster.
              flags:
                --admin-users=: Username (Google email address) of the user who should be granted
                --async: Return immediately, without waiting for the operation in progress to
                --cluster-ipv4-cidr=: All pods in the cluster are assigned an RFC1918 IPv4 address from this
                --container-default-runtime-class=: Name of the default runtime class for containers.
                --control-plane-kms-key=: Google Cloud KMS key that will be used to secure persistent disks of the
                --control-plane-machine-filter=: Only machines matching this filter will be allowed to host
                --control-plane-node-count=: The number of local control plane nodes in a cluster.
                --control-plane-node-location=: Google Edge Cloud zone where the local control plane nodes
                --control-plane-node-storage-schema=: Name for the storage schema of control plane nodes.
                --control-plane-shared-deployment-policy=: Policy configuration about how user application is deployed for
                --default-max-pods-per-node=: The default maximum number of pods per node.
                --enable-google-group-authentication: If set, the cluster will be configured to use Google Group authentication.
                --external-lb-ipv4-address-pools=: IPv4 address pools that are used for data plane load balancing of
                --fleet-project=: Name of the Fleet host project where the cluster is registered.
                --labels=: List of label KEY=VALUE pairs to add.
                --location=: Google Cloud location for the cluster.
                --lro-timeout=: Overwrite the default LRO maximum timeout.
                --maintenance-window-end=: End time of the recurring cluster maintenance window in the RFC 3339
                --maintenance-window-recurrence=: An RFC 5545 (https://tools.ietf.org/html/rfc5545#section-3.8.5.3)
                --maintenance-window-start=: Start time of the recurring cluster maintenance window in the RFC 3339
                --no-async&: Return immediately, without waiting for the operation in progress to
                --no-enable-google-group-authentication&: ""
                --offline-reboot-ttl=: Specifies the maximum duration a node can reboot offline (without
                --release-channel=: Release channel a cluster is subscribed to.
                --services-ipv4-cidr=: All services in the cluster are assigned an RFC1918 IPv4 address from
                --system-addons-config=: If specified as a YAML/JSON file, customized configuration in this file
                --zone-storage-kms-key=: Google Cloud KMS key that will be used to encrypt and decrypt the root key
            - name: delete
              description: Delete an Edge Container cluster.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --location=: The global location name.
                --no-async&: Return immediately, without waiting for the operation in progress to
            - name: describe
              description: Show details about the cluster.
              flags:
                --location=: The global location name.
            - name: get-credentials
              description: Get credentials of an edge-container cluster.
              flags:
                --auth-provider-cmd-path=: Path to the gcloud executable for the auth provider field in kubeconfig.
                --location=: Google Cloud location for the cluster.
                --no-offline-credential&: ""
                --offline-credential: Once specified, an offline credential will be generated for the cluster.
            - name: list
              description: List Edge Container clusters.
              flags:
                --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
                --limit=: Maximum number of resources to list.
                --location=: ID of the location or fully qualified identifier for the location.
                --no-uri&: Print a list of resource URIs instead of the default output, and change the
                --page-size=: Some services group resource list output into pages.
                --sort-by=: Comma-separated list of resource field key names to sort by.
                --uri: Print a list of resource URIs instead of the default output, and change the
            - name: node-pools
              description: Manage Kubernetes Edge node pools.
              commands:
                - name: create
                  description: Create an Edge Container node pool.
                  flags:
                    --async: Return immediately, without waiting for the operation in progress to
                    --cluster=: Cluster of the node pool.
                    --labels=: List of label KEY=VALUE pairs to add.
                    --local-disk-kms-key=: Google Cloud KMS key that will be used to secure local disks on nodes
                    --location=: Google Cloud location for the node pool.
                    --lro-timeout=: Overwrite the default LRO maximum timeout.
                    --machine-filter=: Only machines matching this filter will be allowed to join the node
                    --no-async&: Return immediately, without waiting for the operation in progress to
                    --node-count=!: Default nodeCount used by this node pool.
                    --node-labels=: Comma-delimited list of key-value pairs that comprise labels for the
                    --node-location=!: Google Edge Cloud zone where nodes in this node pool will be created.
                    --node-storage-schema=: Name for the storage schema of worker nodes.
                - name: delete
                  description: Delete an Edge Container nodePool.
                  flags:
                    --async: Return immediately, without waiting for the operation in progress to
                    --cluster=: Kubernetes cluster.
                    --location=: The global location name.
                    --no-async&: Return immediately, without waiting for the operation in progress to
                - name: describe
                  description: Show details about the nodePool.
                  flags:
                    --cluster=: Kubernetes cluster.
                    --location=: The global location name.
                - name: list
                  description: List Edge Container nodePools.
                  flags:
                    --cluster=: ID of the cluster or fully qualified identifier for the cluster.
                    --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
                    --limit=: Maximum number of resources to list.
                    --location=: The global location name.
                    --no-uri&: Print a list of resource URIs instead of the default output, and change the
                    --page-size=: Some services group resource list output into pages.
                    --sort-by=: Comma-separated list of resource field key names to sort by.
                    --uri: Print a list of resource URIs instead of the default output, and change the
                - name: update
                  description: Updates an Edge Container node pool.
                  flags:
                    --async: Return immediately, without waiting for the operation in progress to
                    --clear-labels: Remove all labels.
                    --cluster=: Cluster of the node pool.
                    --local-disk-kms-key=: Google Cloud KMS key that will be used to secure local disks on nodes
                    --location=: Google Cloud location for the node pool.
                    --lro-timeout=: Overwrite the default LRO maximum timeout.
                    --machine-filter=: Only machines matching this filter will be allowed to join the node
                    --no-async&: Return immediately, without waiting for the operation in progress to
                    --no-clear-labels&: Remove all labels.
                    --no-use-google-managed-key&: ""
                    --node-count=: Default nodeCount used by this node pool.
                    --node-labels=: Comma-delimited list of key-value pairs that comprise labels for the
                    --remove-labels=: List of label keys to remove.
                    --update-labels=: List of label KEY=VALUE pairs to update.
                    --use-google-managed-key: Once specified, a Google-managed key will be used for the control plane
            - name: print-access-token
              description: Generate an access token for an Edge Container cluster.
              hidden: true
              flags:
                --location=: Google Cloud location for the cluster.
            - name: update
              description: Update an Edge Container cluster.
              flags:
                --add-maintenance-exclusion-end=: End time of the maintenance exclusion window in the RFC 3339
                --add-maintenance-exclusion-name=: Unique name (per cluster) of the maintenance exclusion window that can be used to remove it.
                --add-maintenance-exclusion-start=: Start time of the maintenance exclusion window (can occur in the past) in the RFC 3339
                --async: Return immediately, without waiting for the operation in progress to
                --clear-maintenance-window: If set, removes the maintenance window setting from the cluster.
                --container-default-runtime-class=: If set, use the specified default container runtime class for the cluster.
                --control-plane-kms-key=: If set, use the specified KMS key for the cluster control plane nodes encryption.
                --location=: The global location name.
                --maintenance-window-end=: End time of the recurring cluster maintenance window in the [RFC 3339](https://www.rfc-editor.org/rfc/rfc3339.txt)
                --maintenance-window-recurrence=: An [RFC 5545](https://tools.ietf.org/html/rfc5545#section-3.8.5.3)
                --maintenance-window-start=: Start time of the recurring cluster maintenance window in the [RFC 3339](https://www.rfc-editor.org/rfc/rfc3339.txt)
                --no-async&: Return immediately, without waiting for the operation in progress to
                --no-clear-maintenance-window&: If set, removes the maintenance window setting from the cluster.
                --no-use-google-managed-key&: If set, use Google managed key for the cluster control plane nodes encryption.
                --no-use-google-managed-zone-key&: If set, use Google managed key for zone storage encryption.
                --offline-reboot-ttl=: Specifies the maximum duration a node can reboot offline (without connection to Google) and
                --release-channel=: Release channel a cluster is subscribed to.
                --remove-maintenance-exclusion-window=: Name of the maintenance exclusion to remove.
                --use-google-managed-key: If set, use Google managed key for the cluster control plane nodes encryption.
                --use-google-managed-zone-key: If set, use Google managed key for zone storage encryption.
                --zone-storage-kms-key=: If set, use the specified KMS key for zone storage encryption.
              completion:
                flag:
                    release-channel:
                        - none
                        - regular
                        - release-channel-unspecified
            - name: upgrade
              description: Upgrade an Edge Container cluster.
              flags:
                --location=: Google Cloud location for the cluster.
                --schedule=!: Schedule to upgrade a cluster after the request is acknowledged by Google.
        - name: get-server-config
          description: Get server config.
          flags:
            --location=: ID of the location or fully qualified identifier for the location.
        - name: machines
          description: Manage Kubernetes Edge machines.
          commands:
            - name: describe
              description: Show details about the machine.
              flags:
                --location=: The global location name.
            - name: list
              description: List Edge Container machines.
              flags:
                --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
                --limit=: Maximum number of resources to list.
                --location=: ID of the location or fully qualified identifier for the location.
                --no-uri&: Print a list of resource URIs instead of the default output, and change the
                --page-size=: Some services group resource list output into pages.
                --sort-by=: Comma-separated list of resource field key names to sort by.
                --uri: Print a list of resource URIs instead of the default output, and change the
        - name: operations
          description: Command group for working with Kubernetes Edge operations.
          commands:
            - name: describe
              description: Get description of a long-running edge container operation.
              flags:
                --location=: The global location name.
            - name: wait
              description: Poll long-running edge container operation until it completes.
              flags:
                --location=: The global location name.
        - name: regions
          description: Manages Edge Container regions.
          commands:
            - name: describe
              description: Describe an Edge Container region (location).
            - name: list
              description: List Edge Container regions.
              flags:
                --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
                --limit=: Maximum number of resources to list.
                --no-uri&: Print a list of resource URIs instead of the default output, and change the
                --page-size=: Some services group resource list output into pages.
                --sort-by=: Comma-separated list of resource field key names to sort by.
                --uri: Print a list of resource URIs instead of the default output, and change the
        - name: vpn-connections
          description: Manage Edge VPN connections between an Edge Container cluster and a VPC network.
          commands:
            - name: create
              description: Create a VPN connection between an Edge Container cluster and a VPC network.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --cluster=!: ID of the cluster or fully qualified identifier for the cluster.
                --high-availability: Enables high availability on cluster side.
                --labels=: List of label KEY=VALUE pairs to add.
                --location=: For resources [cluster, vpn_connection], provides fallback value for resource location attribute.
                --nat-gateway-ip=: The NAT gateway IP for the gateway floating IPs.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --no-high-availability&: Enables high availability on cluster side.
                --router=: Name of the Cloud Router to use when creating the VPN connection.
                --vpc-network=!: The name of the VPC network to be connected.
                --vpc-project=: The project of the VPC network.
            - name: delete
              description: Delete a VPN connection between an Edge Container cluster and a VPC network.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --location=: The global location name.
                --no-async&: Return immediately, without waiting for the operation in progress to
            - name: describe
              description: Show details about a VPN connection.
              flags:
                --location=: The global location name.
            - name: list
              description: List VPN connections.
              flags:
                --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
                --limit=: Maximum number of resources to list.
                --location=: ID of the location or fully qualified identifier for the location.
                --no-uri&: Print a list of resource URIs instead of the default output, and change the
                --page-size=: Some services group resource list output into pages.
                --sort-by=: Comma-separated list of resource field key names to sort by.
                --uri: Print a list of resource URIs instead of the default output, and change the
        - name: zones
          description: Inspect Edge Container zones.
          commands:
            - name: describe
              description: Describe an Edge Container zone.
            - name: list
              description: List Edge Container zones.
              flags:
                --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
                --limit=: Maximum number of resources to list.
                --page-size=: Some services group resource list output into pages.
                --sort-by=: Comma-separated list of resource field key names to sort by.
    - name: networking
      description: Manage Distributed Cloud Edge Network resources.
      commands:
        - name: interconnects
          description: Manage Distributed Cloud Edge Network interconnects.
          commands:
            - name: attachments
              description: Manage Distributed Cloud Edge Network interconnect attachments.
              commands:
                - name: dedicated
                  description: Manage Distributed Cloud Edge Network dedicated interconnect attachments.
                  commands:
                    - name: create
                      description: Create a Distributed Cloud Edge Network interconnect attachment.
                      flags:
                        --async: Return immediately, without waiting for the operation in progress to
                        --description=: An optional, textual description for the interconnect attachment.
                        --interconnect=!: The underlying interconnect object that this attachment's traffic will traverse through.
                        --labels=: List of label KEY=VALUE pairs to add.
                        --location=: The global location name.
                        --mtu=: Maximum transmission unit (MTU) is the size of the largest
                        --network=: The network to use for dynamic routing.
                        --no-async&: Return immediately, without waiting for the operation in progress to
                        --vlan-id=: The ID of the vlan to tag the subnetwork.
                        --zone=: The name of the Google Distributed Cloud Edge zone.
                - name: delete
                  description: Delete a Distributed Cloud Edge Network interconnect attachment.
                  flags:
                    --async: Return immediately, without waiting for the operation in progress to
                    --location=: The global location name.
                    --no-async&: Return immediately, without waiting for the operation in progress to
                    --zone=: The name of the Google Distributed Cloud Edge zone.
                - name: describe
                  description: Show details about a Distributed Cloud Edge Network interconnect attachment.
                  flags:
                    --location=: The global location name.
                    --zone=: The name of the Google Distributed Cloud Edge zone.
                - name: list
                  description: List Distributed Cloud Edge Network interconnect attachments.
                  flags:
                    --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
                    --limit=: Maximum number of resources to list.
                    --location=: The global location name.
                    --no-uri&: Print a list of resource URIs instead of the default output, and change the
                    --page-size=: Some services group resource list output into pages.
                    --sort-by=: Comma-separated list of resource field key names to sort by.
                    --uri: Print a list of resource URIs instead of the default output, and change the
                    --zone=!: ID of the zone or fully qualified identifier for the zone.
            - name: describe
              description: Show details about the Distributed Cloud Edge Network interconnect.
              flags:
                --location=: The global location name.
                --zone=: The name of the Google Distributed Cloud Edge zone.
            - name: get-diagnostics
              description: Get the diagnostics of a specified Distributed Cloud Edge Network interconnect.
              flags:
                --location=: The Cloud location for the interconnect.
                --zone=: The zone of the interconnect.
            - name: list
              description: List Distributed Cloud Edge Network interconnects.
              flags:
                --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
                --limit=: Maximum number of resources to list.
                --location=: The global location name.
                --no-uri&: Print a list of resource URIs instead of the default output, and change the
                --page-size=: Some services group resource list output into pages.
                --sort-by=: Comma-separated list of resource field key names to sort by.
                --uri: Print a list of resource URIs instead of the default output, and change the
                --zone=!: ID of the zone or fully qualified identifier for the zone.
        - name: networks
          description: Manage Distributed Cloud Edge Network networks.
          commands:
            - name: create
              description: Create a Distributed Cloud Edge Network network.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --description=: An optional, textual description for the network.
                --labels=: List of label KEY=VALUE pairs to add.
                --location=: The global location name.
                --mtu=: Maximum transmission unit (MTU) is the size of the largest
                --no-async&: Return immediately, without waiting for the operation in progress to
                --zone=: The name of the Google Distributed Cloud Edge zone.
            - name: delete
              description: Delete a Distributed Cloud Edge Network network.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --location=: The global location name.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --zone=: The name of the Google Distributed Cloud Edge zone.
            - name: describe
              description: Show details about the Distributed Cloud Edge Network network.
              flags:
                --location=: The global location name.
                --zone=: The name of the Google Distributed Cloud Edge zone.
            - name: get-status
              description: Get the status of a specified Distributed Cloud Edge Network network.
              flags:
                --location=: The Cloud location for the network.
                --zone=: The zone of the network.
            - name: list
              description: List Distributed Cloud Edge Network networks.
              flags:
                --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
                --limit=: Maximum number of resources to list.
                --location=: The global location name.
                --no-uri&: Print a list of resource URIs instead of the default output, and change the
                --page-size=: Some services group resource list output into pages.
                --sort-by=: Comma-separated list of resource field key names to sort by.
                --uri: Print a list of resource URIs instead of the default output, and change the
                --zone=!: ID of the zone or fully qualified identifier for the zone.
        - name: operations
          description: Command group for working with Distributed Cloud Edge Network operations.
          commands:
            - name: describe
              description: Get description of a long-running edge network operation.
              flags:
                --location=: The global location name.
            - name: wait
              description: Poll long-running edge network operation until it completes.
              flags:
                --location=: The global location name.
        - name: routers
          description: Manage Distributed Cloud Edge Network routers.
          commands:
            - name: add-bgp-peer
              description: Add a BGP peer to a Distributed Cloud Edge Network router.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --interface=!: The name of the interface for this BGP peer.
                --location=: The Cloud location for the router.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --peer-asn=!: The BGP autonomous system number (ASN) for this BGP peer.
                --peer-ipv4-range=: The IPv4 link-local address range of the peer router.
                --peer-name=!: The name of the new BGP peer being added.
                --zone=: The zone of the router.
            - name: add-interface
              description: Add an interface to a Distributed Cloud Edge Network router.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --interconnect-attachment=: Interconnect attachment of the interface being added.
                --interface-name=!: The name of the interface being added.
                --ip-address=: Link-local address of the router for this interface.
                --ip-mask-length=: Subnet mask for the link-local IP range of the interface.
                --location=: The Cloud location for the router.
                --loopback-ip-addresses=: The list of ip ranges for the loopback interface.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --subnetwork=: Subnetwork of the interface being added.
                --zone=: The zone of the router.
            - name: create
              description: Create a Distributed Cloud Edge Network router.
              flags:
                --asn=!: The locally assigned BGP ASN.
                --async: Return immediately, without waiting for the operation in progress to
                --description=: An optional, textual description for the router.
                --keepalive-interval-in-seconds=&: The interval in seconds between BGP keepalive messages that are sent to the peer.
                --labels=: List of label KEY=VALUE pairs to add.
                --location=: The global location name.
                --network=!: The network that this subnetwork belongs to.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --zone=: The name of the Google Distributed Cloud Edge zone.
            - name: delete
              description: Delete a Distributed Cloud Edge Network router.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --location=: The global location name.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --zone=: The name of the Google Distributed Cloud Edge zone.
            - name: describe
              description: Show details about the Distributed Cloud Edge Network router.
              flags:
                --location=: The global location name.
                --zone=: The name of the Google Distributed Cloud Edge zone.
            - name: get-status
              description: Get the status of a specified Distributed Cloud Edge Network router.
              flags:
                --location=: The Cloud location for the router.
                --zone=: The zone of the router.
            - name: list
              description: List Distributed Cloud Edge Network routers.
              flags:
                --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
                --limit=: Maximum number of resources to list.
                --location=: The global location name.
                --no-uri&: Print a list of resource URIs instead of the default output, and change the
                --page-size=: Some services group resource list output into pages.
                --sort-by=: Comma-separated list of resource field key names to sort by.
                --uri: Print a list of resource URIs instead of the default output, and change the
                --zone=!: ID of the zone or fully qualified identifier for the zone.
            - name: remove-bgp-peer
              description: Remove a BGP peer from a Distributed Cloud Edge Network router.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --location=: The Cloud location for the router.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --peer-name=: The name of the BGP peer being removed.
                --peer-names=: The list of names for peers being removed.
                --zone=: The zone of the router.
            - name: remove-interface
              description: remove an interface on a Distributed Cloud Edge Network router.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --interface-name=: The name of the interface being removed.
                --interface-names=: The list of names for interfaces being removed.
                --location=: The Cloud location for the router.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --zone=: The zone of the router.
        - name: subnets
          description: Manage Distributed Cloud Edge Network subnets.
          commands:
            - name: create
              description: Create a Distributed Cloud Edge Network subnet.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --description=: An optional, textual description for the subnet.
                --ipv4-range=: The ranges of ipv4 addresses that are owned by this subnetwork in CIDR format.
                --ipv6-range=: The ranges of ipv6 addresses that are owned by this subnetwork in CIDR format.
                --labels=: List of label KEY=VALUE pairs to add.
                --location=: The global location name.
                --network=!: The network that this subnetwork belongs to.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --vlan-id=: The ID of the VLAN to tag the subnetwork.
                --zone=: The name of the Google Distributed Cloud Edge zone.
            - name: delete
              description: Delete a Distributed Cloud Edge Network subnet.
              flags:
                --async: Return immediately, without waiting for the operation in progress to
                --location=: The global location name.
                --no-async&: Return immediately, without waiting for the operation in progress to
                --zone=: The name of the Google Distributed Cloud Edge zone.
            - name: describe
              description: Show details about the Distributed Cloud Edge Network subnet.
              flags:
                --location=: The global location name.
                --zone=: The name of the Google Distributed Cloud Edge zone.
            - name: list
              description: List Distributed Cloud Edge Network subnets.
              flags:
                --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
                --limit=: Maximum number of resources to list.
                --location=: The global location name.
                --no-uri&: Print a list of resource URIs instead of the default output, and change the
                --page-size=: Some services group resource list output into pages.
                --sort-by=: Comma-separated list of resource field key names to sort by.
                --uri: Print a list of resource URIs instead of the default output, and change the
                --zone=!: ID of the zone or fully qualified identifier for the zone.
        - name: zones
          description: Manage Distributed Cloud Edge Network zones.
          commands:
            - name: init
              description: Initialize a specified Distributed Cloud Edge Network zone.
              flags:
                --location=: The Cloud location for the zone.
