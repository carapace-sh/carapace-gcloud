# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: network-services
description: Manage Network Services resources.
commands:
    - name: endpoint-policies
      description: Manage Network Services EndpointPolicies.
      commands:
        - name: delete
          description: Delete endpoint policy.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: describe
          description: Describe an endpoint policy.
          flags:
            --location=: The location Id.
        - name: export
          description: Export endpoint policy.
          flags:
            --destination=: Path to a YAML file where the configuration will be exported.
            --location=: The location Id.
        - name: import
          description: Import endpoint policy.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --source=: Path to a YAML file containing the configuration export data.
        - name: list
          description: List endpoint policies.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
    - name: gateways
      description: Manage Network Services Gateways.
      commands:
        - name: delete
          description: Delete a gateway.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: describe
          description: Describe a gateway.
          flags:
            --location=: The location Id.
        - name: export
          description: Export the configuration for a gateway.
          flags:
            --destination=: Path to a YAML file where the configuration will be exported.
            --location=: The location Id.
        - name: import
          description: Import the configuration for a gateway.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --source=: Path to a YAML file containing the configuration export data.
        - name: list
          description: List gateways.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
    - name: grpc-routes
      description: Manage Network Services GrpcRoutes.
      commands:
        - name: delete
          description: Delete grpc route.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: describe
          description: Describe a grpc route.
          flags:
            --location=: The location Id.
        - name: export
          description: Export grpc route.
          flags:
            --destination=: Path to a YAML file where the configuration will be exported.
            --location=: The location Id.
        - name: import
          description: Import grpc route.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --source=: Path to a YAML file containing the configuration export data.
        - name: list
          description: List grpc routes.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
    - name: http-routes
      description: Manage Network Services HttpRoutes.
      commands:
        - name: delete
          description: Delete http route.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: describe
          description: Describe a HTTP route.
          flags:
            --location=: The location Id.
        - name: export
          description: Export http route.
          flags:
            --destination=: Path to a YAML file where the configuration will be exported.
            --location=: The location Id.
        - name: import
          description: Import http route.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --source=: Path to a YAML file containing the configuration export data.
        - name: list
          description: List http routes.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
    - name: meshes
      description: Manage Network Services Meshes.
      commands:
        - name: delete
          description: Delete a mesh.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: describe
          description: Describe a mesh.
          flags:
            --location=: The location Id.
        - name: export
          description: Export a mesh.
          flags:
            --destination=: Path to a YAML file where the configuration will be exported.
            --location=: The location Id.
        - name: import
          description: Import a mesh.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --source=: Path to a YAML file containing the configuration export data.
        - name: list
          description: List meshes.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
    - name: multicast-consumer-associations
      description: Manage Network Services MulticastConsumerAssociations.
      commands:
        - name: create
          description: Create a multicast consumer association.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --description=: The description for the multicast consumer association.
            --labels=: List of label KEY=VALUE pairs to add.
            --location=: The location Id.
            --multicast-domain-activation=!: The multicast domain activation to be used.
            --network=!: The path of the multicast consumer VPC network.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: delete
          description: Delete a multicast consumer association.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: describe
          description: Describe a multicast consumer association.
          flags:
            --location=: The location Id.
        - name: list
          description: List multicast consumer associations.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
        - name: update
          description: Update a multicast consumer association.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --clear-labels: Remove all labels.
            --description=: The description for the multicast consumer association.
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-clear-labels&: Remove all labels.
            --remove-labels=: List of label keys to remove.
            --update-labels=: List of label KEY=VALUE pairs to update.
    - name: multicast-domain-activations
      description: Manage Network Services MulticastDomainActivations.
      commands:
        - name: create
          description: Create a multicast domain activation.
          flags:
            --aggr-egress-pps=!: Aggregated egress Packet-Per-Second for all multicast groups in the domain in this zone.
            --aggr-ingress-pps=: Aggregated ingress Packet-Per-Second for all multicast groups in the domain in this zone.
            --async: Return immediately, without waiting for the operation in progress to
            --avg-packet-size=: Average packet size (defaults to 512 bytes).
            --description=: The description for the multicast domain activation.
            --disable-placement-policy: True to disable the use of the placement policy for this multicast domain activation.
            --labels=: List of label KEY=VALUE pairs to add.
            --location=: The location Id.
            --max-per-group-ingress-pps=: Maximum ingress Packet-Per-Second for a single multicast group in this zone.
            --max-per-group-subscribers=: Maximum number of subscribers for a single multicast group in this zone.
            --multicast-domain=!: The multicast domain to be used.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-disable-placement-policy&: True to disable the use of the placement policy for this multicast domain activation.
        - name: delete
          description: Delete a multicast domain activation.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: describe
          description: Describe a multicast domain activation.
          flags:
            --location=: The location Id.
        - name: list
          description: List multicast domain activations.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
        - name: update
          description: Update a multicast domain activation.
          flags:
            --aggr-egress-pps=: Aggregated egress packets per second for all multicast groups in the domain in this zone.
            --aggr-ingress-pps=: Aggregated ingress Packet-Per-Second for all multicast groups in the domain in this zone.
            --async: Return immediately, without waiting for the operation in progress to
            --avg-packet-size=: Average packet size (defaults to 512 bytes).
            --clear-labels: Remove all labels.
            --description=: The description for the multicast domain activation.
            --location=: The location Id.
            --max-per-group-ingress-pps=: Maximum ingress Packet-Per-Second for a single multicast group in this zone.
            --max-per-group-subscribers=: Maximum number of subscribers for a single multicast group in this zone.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-clear-labels&: Remove all labels.
            --remove-labels=: List of label keys to remove.
            --update-labels=: List of label KEY=VALUE pairs to update.
    - name: multicast-domain-groups
      description: Manage Network Services MulticastDomainGroups.
      commands:
        - name: create
          description: Create a multicast domain group.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --description=: The description for the multicast domain group.
            --labels=: List of label KEY=VALUE pairs to add.
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: delete
          description: Delete a multicast domain group.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: describe
          description: Describe a multicast domain group.
          flags:
            --location=: The location Id.
        - name: list
          description: List multicast domain groups.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
        - name: update
          description: Update a multicast domain group.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --clear-labels: Remove all labels.
            --description=: The description for the multicast domain group.
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-clear-labels&: Remove all labels.
            --remove-labels=: List of label keys to remove.
            --update-labels=: List of label KEY=VALUE pairs to update.
    - name: multicast-domains
      description: Manage Network Services MulticastDomains.
      commands:
        - name: create
          description: Create a multicast domain.
          flags:
            --admin-network=!: The URI of the admin network to be used.
            --async: Return immediately, without waiting for the operation in progress to
            --connection-type=: The connection type for authorizing multicast traffic.
            --description=: The description for the multicast domain.
            --labels=: List of label KEY=VALUE pairs to add.
            --location=: The location Id.
            --multicast-domain-group=: The URI of the multicast domain group to be used.
            --ncc-hub=: The URI of the NCC hub to be used.
            --no-async&: Return immediately, without waiting for the operation in progress to
          completion:
            flag:
                connection-type:
                    - connection-type-unspecified
                    - ncc
                    - same-vpc
        - name: delete
          description: Delete a multicast domain.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: describe
          description: Describe a multicast domain.
          flags:
            --location=: The location Id.
        - name: list
          description: List multicast domains.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
        - name: update
          description: Update a multicast domain.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --clear-labels: Remove all labels.
            --description=: The description for the multicast domain.
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-clear-labels&: Remove all labels.
            --remove-labels=: List of label keys to remove.
            --update-labels=: List of label KEY=VALUE pairs to update.
    - name: multicast-group-consumer-activations
      description: Manage Network Services MulticastGroupConsumerActivations.
      commands:
        - name: create
          description: Create a multicast group consumer activation.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --description=: The description for the multicast group consumer activation.
            --enable-logging: Whether to enable logging for this multicast group consumer activation.
            --labels=: List of label KEY=VALUE pairs to add.
            --location=: The location Id.
            --multicast-consumer-association=!: The multicast consumer association to be used.
            --multicast-group-range-activation=: The multicast group range activation to be used.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-enable-logging&: Whether to enable logging for this multicast group consumer activation.
        - name: delete
          description: Delete a multicast group consumer activation.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: describe
          description: Describe a multicast group consumer activation.
          flags:
            --location=: The location Id.
        - name: list
          description: List multicast group consumer activations.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
        - name: update
          description: Update a multicast group consumer activation.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --clear-labels: Remove all labels.
            --description=: The description for the multicast group consumer activation.
            --enable-logging: Whether to enable logging for this multicast group consumer activation.
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-clear-labels&: Remove all labels.
            --no-enable-logging&: Whether to enable logging for this multicast group consumer activation.
            --remove-labels=: List of label keys to remove.
            --update-labels=: List of label KEY=VALUE pairs to update.
    - name: multicast-group-producer-activations
      description: Manage Network Services MulticastGroupProducerActivations.
      commands:
        - name: create
          description: Create a multicast group producer activation.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --description=: The description for the multicast group producer activation.
            --labels=: List of label KEY=VALUE pairs to add.
            --location=: The location Id.
            --multicast-group-range-activation=: The multicast group range activation to be used.
            --multicast-producer-association=!: The multicast producer association to be used.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: delete
          description: Delete a multicast group producer activation.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: describe
          description: Describe a multicast group producer activation.
          flags:
            --location=: The location Id.
        - name: list
          description: List multicast group producer activations.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
        - name: update
          description: Update a multicast group producer activation.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --clear-labels: Remove all labels.
            --description=: The description for the multicast group producer activation.
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-clear-labels&: Remove all labels.
            --remove-labels=: List of label keys to remove.
            --update-labels=: List of label KEY=VALUE pairs to update.
    - name: multicast-group-range-activations
      description: Manage Network Services MulticastGroupRangeActivations.
      commands:
        - name: create
          description: Create a multicast group range activation.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --description=: The description for the multicast group.
            --enable-logging: Whether to enable logging for this multicast group range activation.
            --labels=: List of label KEY=VALUE pairs to add.
            --location=: The location Id.
            --multicast-domain-activation=!: The multicast domain activation to be used.
            --multicast-group-range=!: The multicast group range to be used.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-enable-logging&: Whether to enable logging for this multicast group range activation.
        - name: delete
          description: Delete a multicast group range activation.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: describe
          description: Describe a multicast group range activations.
          flags:
            --location=: The location Id.
        - name: list
          description: List multicast group range activations.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
        - name: update
          description: Update a multicast group range activation.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --clear-labels: Remove all labels.
            --description=: The description for the multicast group range activation.
            --enable-logging: Whether to enable logging for this multicast group range activation.
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-clear-labels&: Remove all labels.
            --no-enable-logging&: Whether to enable logging for this multicast group range activation.
            --remove-labels=: List of label keys to remove.
            --update-labels=: List of label KEY=VALUE pairs to update.
    - name: multicast-group-ranges
      description: Manage Network Services MulticastGroupRanges.
      commands:
        - name: create
          description: Create a multicast group range.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --consumer-accept-list=: An optional list of consumer projects that can use this multicast group range.
            --description=: The description for the multicast group range.
            --distribution-scope=: Distribution scope of this multicast group range.
            --enable-logging: Whether to enable logging for this multicast group range.
            --labels=: List of label KEY=VALUE pairs to add.
            --location=: The location Id.
            --multicast-domain=!: The multicast domain to be used.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-enable-logging&: Whether to enable logging for this multicast group range.
            --no-require-explicit-accept&: Whether an empty consumer accept list will reject all consumer projects.
            --require-explicit-accept: Whether an empty consumer accept list will reject all consumer projects.
            --reserved-internal-range=!: The reserved internal range to be used.
          completion:
            flag:
                distribution-scope:
                    - distribution-scope-unspecified
                    - intra-region
                    - intra-zone
        - name: delete
          description: Delete a multicast group range.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: describe
          description: Describe a multicast group range.
          flags:
            --location=: The location Id.
        - name: list
          description: List multicast group ranges.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
        - name: update
          description: Update a multicast group range.
          flags:
            --add-consumer-accept-list=: Add new value to consumer_accept_list list.
            --async: Return immediately, without waiting for the operation in progress to
            --clear-consumer-accept-list: Clear consumer_accept_list value and set to empty list.
            --clear-labels: Remove all labels.
            --consumer-accept-list=: Set consumer_accept_list to new value.
            --description=: The description for the multicast group range.
            --enable-logging: Whether to enable logging for this multicast group range.
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-clear-consumer-accept-list&: Clear consumer_accept_list value and set to empty list.
            --no-clear-labels&: Remove all labels.
            --no-enable-logging&: Whether to enable logging for this multicast group range.
            --no-require-explicit-accept&: Whether an empty consumer accept list will reject all consumer projects.
            --remove-consumer-accept-list=: Remove existing value from consumer_accept_list list.
            --remove-labels=: List of label keys to remove.
            --require-explicit-accept: Whether an empty consumer accept list will reject all consumer projects.
            --update-labels=: List of label KEY=VALUE pairs to update.
    - name: multicast-producer-associations
      description: Manage Network Services MulticastProducerAssociations.
      commands:
        - name: create
          description: Create a multicast producer association.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --description=: The description for the multicast producer association.
            --labels=: List of label KEY=VALUE pairs to add.
            --location=: The location Id.
            --multicast-domain-activation=!: The multicast domain activation to be used.
            --network=!: The path of the multicast producer VPC network.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: delete
          description: Delete a multicast producer association.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: describe
          description: Describe a multicast producer associations.
          flags:
            --location=: The location Id.
        - name: list
          description: List multicast producer associations.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
        - name: update
          description: Update a multicast producer association.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --clear-labels: Remove all labels.
            --description=: The description for the multicast producer association.
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-clear-labels&: Remove all labels.
            --remove-labels=: List of label keys to remove.
            --update-labels=: List of label KEY=VALUE pairs to update.
    - name: operations
      description: Manage Network Services Operations.
      commands:
        - name: cancel
          description: Cancel a Network Services long running operation.
          flags:
            --location=: The location Id.
        - name: describe
          description: Show details of a Network Services long running operation.
          flags:
            --location=: The location Id.
        - name: list
          description: List all Network Services long running operations.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
        - name: wait
          description: Poll a Network Services long running operation.
          flags:
            --location=: The location Id.
    - name: route-views
      description: View Network Services Route Views.
      commands:
        - name: describe
          description: Route View for a Mesh or Gateway.
          flags:
            --gateway=: Parent Gateway of the mesh_or_gateway_route_view
            --location=: Location of the mesh_or_gateway_route_view
            --mesh=: Parent Mesh of the mesh_or_gateway_route_view
            --route-view=!: ID of the mesh_or_gateway_route_view or fully qualified identifier for the mesh_or_gateway_route_view.
        - name: list
          description: Route View for a Mesh or Gateway.
          flags:
            --gateway=!: ID of the gateway or fully qualified identifier for the gateway.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --mesh=!: ID of the mesh or fully qualified identifier for the mesh.
            --page-size=: Some services group resource list output into pages.
    - name: service-bindings
      description: Manage Network Services Bindings.
      commands:
        - name: create
          description: Create a service binding.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --description=: The description for the service binding.
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: delete
          description: Delete service binding.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: describe
          description: Describe a service binding.
          flags:
            --location=: The location Id.
        - name: export
          description: Export a service binding.
          flags:
            --destination=: Path to a YAML file where the configuration will be exported.
            --location=: The location Id.
        - name: import
          description: Import a service binding.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --source=: Path to a YAML file containing the configuration export data.
        - name: list
          description: List service bindings.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
        - name: update
          description: Update a service binding.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --description=: The description for the service binding.
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
    - name: service-lb-policies
      description: Manage Network Services ServiceLbPolicies.
      commands:
        - name: create
          description: Create a service LB policy.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --auto-capacity-drain: If specified, an unhealthy MIG/NEG will be removed from Global load balancing and traffic
            --description=: The description for the service LB policy.
            --failover-health-threshold=: The percentage threshold that a load balancer will begin to send traffic to failover backends.
            --isolation-config-granularity=: The granularity of the isolation.
            --isolation-config-mode=: The granularity of the isolation.
            --load-balancing-algorithm=: The global load balancing algorithm to be used.
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-auto-capacity-drain&: If specified, an unhealthy MIG/NEG will be removed from Global load balancing and traffic
          completion:
            flag:
                isolation-config-granularity:
                    - region
                    - unspecified
                isolation-config-mode:
                    - nearest
                    - strict
                    - unspecified
                load-balancing-algorithm:
                    - spray-to-region
                    - spray-to-world
                    - waterfall-by-region
                    - waterfall-by-zone
        - name: delete
          description: Delete service LB policy.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: describe
          description: Describe a service LB policy.
          flags:
            --location=: The location Id.
        - name: export
          description: Export service LB policy.
          flags:
            --destination=: Path to a YAML file where the configuration will be exported.
            --location=: The location Id.
        - name: import
          description: Import service LB policy.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --source=: Path to a YAML file containing the configuration export data.
        - name: list
          description: List ServiceLbPolicies.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
        - name: update
          description: Update a service LB policy.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --auto-capacity-drain: If specified, an unhealthy MIG/NEG will be removed from Global load balancing and traffic
            --description=: The description for the service LB policy.
            --failover-health-threshold=: The percentage threshold that a load balancer will begin to send traffic to failover backends.
            --isolation-config-granularity=: The granularity of the isolation.
            --isolation-config-mode=: The granularity of the isolation.
            --load-balancing-algorithm=: The global load balancing algorithm to be used.
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-auto-capacity-drain&: If specified, an unhealthy MIG/NEG will be removed from Global load balancing and traffic
          completion:
            flag:
                isolation-config-granularity:
                    - region
                    - unspecified
                isolation-config-mode:
                    - nearest
                    - strict
                    - unspecified
                load-balancing-algorithm:
                    - spray-to-region
                    - spray-to-world
                    - waterfall-by-region
                    - waterfall-by-zone
    - name: tcp-routes
      description: Manage Network Services TcpRoutes.
      commands:
        - name: delete
          description: Delete tcp route.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: describe
          description: Describe a TCP route.
          flags:
            --location=: The location Id.
        - name: export
          description: Export tcp route.
          flags:
            --destination=: Path to a YAML file where the configuration will be exported.
            --location=: The location Id.
        - name: import
          description: Import tcp route.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --source=: Path to a YAML file containing the configuration export data.
        - name: list
          description: List tcp routes.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
    - name: tls-routes
      description: Manage Network Services TlsRoutes.
      commands:
        - name: delete
          description: Delete tls route.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
        - name: describe
          description: Describe a tls route.
          flags:
            --location=: The location Id.
        - name: export
          description: Export tls route.
          flags:
            --destination=: Path to a YAML file where the configuration will be exported.
            --location=: The location Id.
        - name: import
          description: Import tls route.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --location=: The location Id.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --source=: Path to a YAML file containing the configuration export data.
        - name: list
          description: List tls routes.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --location=!: ID of the location or fully qualified identifier for the location.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
