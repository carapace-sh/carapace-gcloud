# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: source-manager
description: Manage Secure Source Manager resources.
commands:
    - name: instances
      description: Manage Secure Source Manager instances.
      commands:
        - name: add-iam-policy-binding
          description: Add an IAM policy binding to a Secure Source Manager instance.
          flags:
            --member=!: The principal to add the binding for.
            --region=: Secure Source Manager location.
            --role=!: Role name to assign to the principal.
        - name: create
          description: Create a Secure Source Manager instance.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --ca-pool=: CA Pool path for private instance.
            --enable-workforce-identity-federation: Bool indicator for workforce identity federation instance.
            --is-private!: Bool indicator for private instance.
            --kms-key=: KMS key used to encrypt instance optionally.
            --max-wait=: Time to synchronously wait for the operation to complete, after which the operation continues asynchronously.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --no-enable-workforce-identity-federation&: Bool indicator for workforce identity federation instance.
            --psc-allowed-projects=: List of additional projects allowed to connect to the instance via private service connect.
            --region=: Secure Source Manager location.
        - name: delete
          description: Delete a Secure Source Manager instance.
          flags:
            --async: Return immediately, without waiting for the operation in progress to
            --max-wait=: Time to synchronously wait for the operation to complete, after which the operation continues asynchronously.
            --no-async&: Return immediately, without waiting for the operation in progress to
            --region=: Secure Source Manager location.
        - name: describe
          description: Describe a Secure Source Manager instance.
          flags:
            --region=: Secure Source Manager location.
        - name: get-iam-policy
          description: Get the IAM policy for a Secure Source Manager instance.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --page-size=: Some services group resource list output into pages.
            --region=: Secure Source Manager location.
            --sort-by=: Comma-separated list of resource field key names to sort by.
        - name: list
          description: List Secure Source Manager instances.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --region=!: ID of the location or fully qualified identifier for the location.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
        - name: remove-iam-policy-binding
          description: Remove an IAM policy binding to a Secure Source Manager instance.
          flags:
            --member=!: The principal to remove the binding for.
            --region=: Secure Source Manager location.
            --role=!: The role to remove the principal from.
        - name: set-iam-policy
          description: Set the IAM policy for a Secure Source Manager instance.
          flags:
            --region=: Secure Source Manager location.
    - name: locations
      description: Manage Secure Source Manager locations.
      commands:
        - name: list
          description: List Secure Source Manager locations.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
    - name: operations
      description: Manage Secure Source Manager operations.
      commands:
        - name: describe
          description: Describe a Secure Source Manager operation.
          flags:
            --region=: Secure Source Manager location.
        - name: list
          description: List Secure Source Manager operations.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --no-uri&: Print a list of resource URIs instead of the default output, and change the
            --page-size=: Some services group resource list output into pages.
            --region=!: ID of the location or fully qualified identifier for the location.
            --sort-by=: Comma-separated list of resource field key names to sort by.
            --uri: Print a list of resource URIs instead of the default output, and change the
    - name: repos
      description: Manage Secure Source Manager repositories.
      commands:
        - name: add-iam-policy-binding
          description: Add an IAM policy binding to a Secure Source Manager repository.
          flags:
            --member=!: The principal to add the binding for.
            --region=: Secure Source Manager location.
            --role=!: Role name to assign to the principal.
        - name: create
          description: Create a Secure Source Manager repository.
          flags:
            --default-branch=: Default branch name of the repository.
            --description=: Description of the repository.
            --gitignores=: List of gitignore template names user can choose from.
            --instance=!: A Secure Source Manager instance ID.
            --license=: License template name user can choose from.
            --readme=: README template name.
            --region=: Secure Source Manager location.
        - name: delete
          description: Delete a Secure Source Manager repository.
          flags:
            --allow-missing: If set to true, and the resource is not found, the request will succeed but no action will be taken on the server.
            --no-allow-missing&: If set to true, and the resource is not found, the request will succeed but no action will be taken on the server.
            --region=: Secure Source Manager location.
        - name: describe
          description: Describe a Secure Source Manager repository.
          flags:
            --region=: Secure Source Manager location.
        - name: get-iam-policy
          description: Get the IAM policy for a Secure Source Manager repository.
          flags:
            --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            --limit=: Maximum number of resources to list.
            --page-size=: Some services group resource list output into pages.
            --region=: Secure Source Manager location.
            --sort-by=: Comma-separated list of resource field key names to sort by.
        - name: list
          description: List all repositories under a Secure Source Manager instance.
          flags:
            --instance=!: A Secure Source Manager instance ID.
            --limit=: Maximum number of resources to list.
            --page-size=: Some services group resource list output into pages.
            --region=!: ID of the location or fully qualified identifier for the location.
        - name: remove-iam-policy-binding
          description: Remove an IAM policy binding from a Secure Source Manager repository.
          flags:
            --member=!: The principal to remove the binding for.
            --region=: Secure Source Manager location.
            --role=!: The role to remove the principal from.
        - name: set-iam-policy
          description: Set the IAM policy for a Secure Source Manager repository.
          flags:
            --region=: Secure Source Manager location.
        - name: update
          description: Update a Secure Source Manager repository.
          flags:
            --description=: Description of the repository.
            --no-validate-only&: If set to true, the request is validated and the user is provided with an expected result, but no actual change is made.
            --region=: Secure Source Manager location.
            --validate-only: If set to true, the request is validated and the user is provided with an expected result, but no actual change is made.
